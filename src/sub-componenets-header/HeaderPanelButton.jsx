import styled from "styled-components";

const HeaderPanelButton = styled.img`
  background: rgba(255, 255, 255, 0.16);
  backdrop-filter: blur(8px);
  border-radius: 50%;
`;

export default HeaderPanelButton;
